"""trialcat backend application package.

The web layer that serves the map, aggregates clinical trial data,
and hosts the API surface. Like Cairn's navigation-based memory
finding its way through a forest of nodes, this package finds
structure in a forest of clinical trials.
"""

__version__ = "0.1.0"
