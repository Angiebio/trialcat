# trialcat backend dependencies
# Pinned minor versions for reproducibility; bump deliberately.

# --- Web framework ---
fastapi==0.115.5
uvicorn[standard]==0.32.1

# --- Config & validation ---
pydantic>=2.11.0,<3.0.0
pydantic-settings>=2.10.1,<3.0.0

# --- HTTP client for CT.gov API (Phase 2) ---
httpx==0.28.1

# --- Rate limiting (Phase 6) ---
slowapi==0.1.9

# --- Templating (for serving index.html with variables if needed later) ---
jinja2==3.1.4

# --- Database (Phase 2) ---
sqlalchemy==2.0.36

# --- Dev / testing ---
pytest==8.3.4
pytest-asyncio==0.24.0
httpx[cli]==0.28.1
